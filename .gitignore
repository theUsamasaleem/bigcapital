node_modules/

# Docker volumes data directory
/data

# Production env file
.env

# Database dumps / backups — contain real emails + password hashes, never commit
backups/
*.sql

test-results/
.qodo


.pnpm-store